# 粘贴功能偶尔失效

## 原因

在 Linux 上，系统默认不允许应用程序抢占光标焦点，这可能会导致粘贴功能表现不佳。

## 解决方案

将阻止焦点抢占的级别设置为**低**。

### KDE 桌面环境

1. 打开 **系统设置**。
2. 进入 **窗口管理** > **窗口行为**。
3. 找到 **阻止焦点抢占**，将其设置为 **低**。

<Image src="/problem/linux/unfocused/unfocused-light.gif" />
