# 应用启动失败

## 原因

EcoPaste 依赖 `WebKitGTK`，如果缺少必要的依赖项，会导致启动失败。

## 解决方案

请确保已安装所需的依赖项。

### ArchLinux / Manjaro

```bash
sudo pacman -S webkit2gtk-4.1
```

### Debian / Ubuntu

```bash
sudo apt-get install libwebkit2gtk-4.0-dev
```

### Gentoo

```bash
sudo emerge --ask net-libs/webkit-gtk:4.1
```
